// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  output: "static",
  base: "/web-menus/", // reemplazá con el nombre real del repo
});
